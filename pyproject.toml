[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Manual General Contigo"
authors = [{name = "Manexware", email = "info@manexware.com"}]
dynamic = ["version", "description"]
